import { defineConfig } from 'astro/config'

export default defineConfig({
    site: 'https://evegeniynekrasov.github.io',
    image: {
        layout: 'constrained',
        responsiveStyles: true,
    },
})
